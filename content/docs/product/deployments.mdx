---
title: Deployments
description: The isolation boundary used by the frontend SDK and runtime services.
---

A deployment is the unit that binds together:

- frontend host
- backend host
- auth settings
- organization and workspace behavior
- webhook, notification, and API auth configuration

In practice, deployment configuration is the bridge between console configuration and runtime behavior in frontend integrations.
